# Wave 6 Requirements
# Real MCP Server Composition with SmolAgents Testing

# Core dependencies with specific versions for required features
fastmcp>=2.7.0  # Minimum version for as_proxy() with config dict support (latest: 2.9.0)
smolagents[litellm,mcp]>=1.19.0  # With LiteLLM and MCP extras for Ollama and MCP client support

# Python version requirement
# Requires Python 3.10+ for FastMCP

# Optional but recommended for better async handling
uvloop>=0.19.0

# The MCP servers we're testing are installed via npx, so ensure Node.js is available
# - perplexity-mcp (via npx)
# - @modelcontextprotocol/server-sequential-thinking (via npx)

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiohttp>=3.9.0
requests>=2.31.0  # For server startup check